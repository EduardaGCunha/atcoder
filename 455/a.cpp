#include <bits/stdc++.h>
using namespace std;

#define int long long
#define fastio ios::sync_with_stdio(false);cin.tie(nullptr);
#define endl '\n'

signed main(){
    int a, b, c; cin >> a >> b >> c;
    if(a != b && b == c) cout << "Yes\n";
    else cout << "No\n";
}